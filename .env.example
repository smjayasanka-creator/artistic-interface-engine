SUPABASE_PROJECT_ID=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Shared secret for pg_cron / scheduler workers under src/routes/api/public/hooks/*.
# Sent as the `X-Cron-Secret` header. Generate a long random value and set it
# identically here and in the scheduler's job config — never reuse the
# Supabase publishable/anon key for this, it is public.
CRON_SECRET=""
